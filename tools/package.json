{
  "name": "tools",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "node --require ts-node/register --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "chordsheetjs": "^12.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.12",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
