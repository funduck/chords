import { Text } from "@mantine/core";

export default function PublicSearchDisclaimer() {
  return (
    <Text c="dimmed" m="md">
      Note: The public library has just a couple of songs, so you can get a feel of the app. There are currently no
      plans to add more songs to the public library, not least because of copyright concerns.
    </Text>
  );
}
